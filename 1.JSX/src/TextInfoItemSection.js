export const TextInfoItemSection = (props) => {
  return (
    <div className={props.divClassName}>
    <div className={props.tmDivClassName}>                                
        <p className="tm-text">{props.value}</p>
    </div>    
</div>
  )
} 